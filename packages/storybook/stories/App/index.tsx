import * as React from 'react';
import { App } from 'tquinlan92-material-core-ui';
import { storiesOf } from '@storybook/react';

storiesOf('App', module)
  .add('with some folders', () => (
    <App>
    </App>
  ));
